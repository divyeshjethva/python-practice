
# import pymysql

# connection = pymysql.connect (
#     host="localhost",
#     user="root",
#     password="",
#     database="my"
# )
# mycursor = connection.cursor()

# insert_query = "INSERT INTO students (name, age, course) VALUES (%s, %s, %s)"
# values = ("ajay", 21, 20000)

# mycursor.execute(insert_query,values)

# connection.commit()
