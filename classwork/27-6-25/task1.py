# import pymysql

# connection = pymysql.connect(
#     host= "localhost",
#     user="root",
#     password="",
#     database="manager"
# )

# cursor = connection.cursor()
# cursor.execute("CREATE TABLE IF NOT EXISTS cmp (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255), age INT)")


# connection.commit()
